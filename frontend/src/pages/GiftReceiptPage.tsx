/**
 * GiftReceiptPage — route wrapper for the gift receipt.
 * Mounted at /gifts/:giftId
 */

import React from 'react';
import GiftReceipt from '../components/tip/GiftReceipt';

const GiftReceiptPage: React.FC = () => <GiftReceipt />;

export default GiftReceiptPage;
